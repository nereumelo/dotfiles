# Recommended /etc/wsl.conf for Arch WSL (Cursor/Windows CLIs on PATH).
# install.sh does NOT rewrite this file. Apply manually or via bootstrap.sh,
# then restart the distro: wsl --shutdown (from Windows).

[boot]
systemd=true

[interop]
appendWindowsPath=true
