# Managed by chezmoi. Host blocks live in ~/.ssh/config.local (not tracked):
#   HostName, User, IdentityAgent, IdentitiesOnly, IdentityFile
# ssh-host-local <host> <hostname> <user>
# ssh-pub <host> <key>
Include config.local
