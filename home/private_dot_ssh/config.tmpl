# Managed by chezmoi.
# HostName, User, IdentityAgent, IdentitiesOnly → ~/.ssh/config.local (not tracked)
# IdentityFile → ssh-manage Set Public Key writes ~/.ssh/config.identity; inlined here.
# ssh-manage  (interactive: Set Host / Set Public Key)
Include config.local
{{- $idf := joinPath .chezmoi.homeDir ".ssh/config.identity" -}}
{{- if stat $idf }}

{{ trim (output "cat" $idf) }}
{{- end }}
