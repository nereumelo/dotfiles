# Managed by chezmoi.
# ssh-manage Set Host → ~/.ssh/config.local (HostName, User, IdentityAgent, IdentitiesOnly; not tracked).
# ssh-manage Set Public Key → ~/.ssh/config.identity, inlined below.
# setup-work → [git.<org>] in chezmoi (Git insteadOf + identity), not this file.
Include config.local
{{- $idf := joinPath .chezmoi.homeDir ".ssh/config.identity" -}}
{{- if stat $idf }}

{{ trim (output "cat" $idf) }}
{{- end }}
